/* Your code needs to at least start here. */
#include <iostream>

int main() {
    // Print a simple message
    std::cout << "Hello, World!"; 

    // Print a message followed by a new line
    std::cout << "This is on a new line." << std::endl; 
    
    // You can also use the newline character '\n'
    std::cout << "Another line with a newline character.\n"; 

    // Print variables
    int age = 25;
    std::cout << "My age is: " << age << std::endl;

    return 0;
}
